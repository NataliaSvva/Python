with open('text_6.txt', 'r', encoding="utf-8")as text:
    def less(less89):
        a = less.split()
        my_object = a[0]
        a.pop(0)
        my_sum = []
        num = ""
        for k in a:
            word = list(k)
            for e in word:
                if"0" <= e <= "9":
                    num += e
            if num != "":
                num = int(num)
                my_sum.append(num)
            my_sum = ass

    print(ass)

less()